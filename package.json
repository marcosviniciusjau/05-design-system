{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=!docs && npm publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.5",
    "@types/react-input-mask": "^3.0.5",
    "turbo": "^1.5.5"
  },
  "dependencies": {
    "react-input-mask": "^2.0.4"
  }
}
